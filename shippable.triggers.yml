triggers:
  - name: trigger-deploy-test
    type: trigger
    version:
      message: 'testing trigger in test - 2'
    flags:
      - hello
  - name: trigger-deploy-prod
    type: trigger
    version:
      message: 'testing trigger in prod'
    flags:
      - hello
